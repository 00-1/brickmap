[package]
name = "bm-core"
version = "0.0.1"
edition = "2021"
rust-version = "1.94"
description = "brickmap engine: dependency-light foundation — shared math/POD glue + small utils."
license = "MIT OR Apache-2.0"
repository = "https://github.com/00-1/brickmap"

[dependencies]
glam = "0.33"
bytemuck = { version = "1", features = ["derive"] }
