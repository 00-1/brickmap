# Rust build artifacts
/target

# Generated web bundle (produced by `wasm-bindgen`; see README "Running on the web")
/web/pkg

# Editor / OS noise
*.swp
.DS_Store

# Claude Code agent worktrees / session artifacts (tooling, not project content)
.claude/
